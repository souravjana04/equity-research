const ConcallBadge = () => {
  return (
    <span className="font-ui text-[10px] font-semibold leading-[12px] tracking-[0.05em] uppercase bg-info-bg border border-info-border text-info rounded-sm px-2 py-1 inline-flex items-center justify-center">
      CONCALL
    </span>
  );
};

export default ConcallBadge;
